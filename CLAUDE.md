# このリポジトリで Claude が守ること（実験用）

- 事実の正はこのリポジトリの `kb/` 配下。Notion は写し。
- `main` には直接 push しない。`tools/propose.sh` で PR を作る。検査 `kb-check` が緑でなければ入らない。
- 結論・決定を書くときは、依存する前提を列挙し、各前提に「実測（日付）／出典／未確認」を付ける。付けられないものは仮説として書く。
- 状態ファイル（`type: state`）は上書きのみ。失効印を書かない。履歴は git log と `kb/decisions/` が持つ。
